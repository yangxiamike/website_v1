# 项目开发日志

只记录三类信息：

- 做完了什么
- 现在卡在哪
- 项目目前到什么阶段

---

## 2026-05-29

- 仓库已经清理过一轮，项目体积明显下降，目录也更清楚了。
- 首页和表单页的手机端体验已经做过一轮修正，基础可用性更好。
- 多个子页面的手机端样式和交互还在统一中，方向已定，但还没完全收尾。
- 开发版站点和生成后的客户站点已经区分清楚，后面不容易再混用。
- 模板站这轮手机端修改已经开始从“看到哪里改哪里”收口成统一规则，公共样式和公共组件层都补了一轮。
- `index.css` 里新增了手机端 token 和公共 class，横向滑动区、统计卡、按钮高度、图片比例、卡片宽度不再完全散在页面里。
- 公共组件层补进了通用 CTA、横向滑动区、移动端卡片壳和统计卡组件，首页、关于页和部分详情页已经开始接入共享模式。
- `kimi-b2b-demo-polisher` skill 已同步补上手机端收尾规则，后面做类似模板时不容易再次改散。
- 模板站依赖已补装，当前 `build` 和 `lint` 都通过，主线编译状态稳定。
- 生成站同步风险仍然存在：模板更新后，已有 `sites/<company-id>/app` 不会自动完整刷新，后面要单独处理。
- 已对上一轮“手机端整合误伤”做回收：把多个被误改成平铺网格的 mobile marquee、横向滑动卡片和 `About` 时间线，改回统一的移动端 rail / marquee 模式。
- 这次不是零碎补丁，而是把共享层补成 `MobileRail` / `MobileMarquee`，再让 `About`、`Factory`、`ProductDetail`、`Industries`、`IndustryDetail`、`Cases`、`CaseDetail`、`Resources` 接回正确的手机端交互。

### 当天状态

- 已完成：仓库清理、首页手机端修正、站点分层说明、手机端规则第一轮收口、模板站构建验证
- 已完成：误改 mobile 交互的统一回收、时间线恢复、构建和 lint 再验证
- 待补：更完整的移动端截图矩阵复核

---

## 2026-05-28

- React 模板已经开始和统一数据结构接轨，后面可以更顺地接生成系统。
- Python 生成系统已经落地，项目具备“根据公司资料生成网站”的基础能力。
- 多模板、不同资料详细度、生成后再定制这些方向已经确定，后面继续扩展。
- 项目规则已经补上，大改动前先存计划，做完后记日志。

### 当天状态

- 已完成：生成系统基础版、项目流程规范
- 部分完成：模板和数据结构接轨
- 已确定方向：多模板生成能力扩展

---

## 当前项目状态

- 项目已经不只是一个前端页面，而是开始形成“模板 + 数据 + 生成”的建站体系。
- 现在基础框架已经有了，接下来主要是在两条线上继续推进：
  - 把生成能力做完整
  - 把前端页面，尤其是手机端体验，继续打磨统一
- 当前最明显的未完成项，是子页面手机端统一的收尾和最终验证。
